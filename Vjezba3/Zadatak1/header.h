#include <cstddef>
#include <iostream>
#include <vector>
using namespace std;

void vctinp(vector<int>& niz, int nrelem);
void vctinp2(vector<int>& niz, int p, int k);
void vctprint(vector<int>& niz);
